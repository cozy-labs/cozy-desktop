/* @flow */

/*::
import type Buffer from './buffer'

export type Scanner = (string) => Promise<void>

export interface Producer {
  buffer: Buffer,
  +scan: Scanner,
  start(): Promise<*>,
  stop(): *
}
*/
